import ReactDOM from 'react-dom/client';

import './index.css';
//app is a component that gets rendered inside of this div
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
